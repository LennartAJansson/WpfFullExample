<UserControl x:Class="WpfApp1.Views.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:viewmodels="clr-namespace:WpfApp1.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="800">
    <Menu>
        <MenuItem Header="File">
            <MenuItem Header="Show People" Command="{Binding PeopleViewCommand}" />
            <MenuItem Header="Show Person" Command="{Binding PersonViewCommand}" />
            <MenuItem Header="Show Graph" Command="{Binding GraphViewCommand}" />
        </MenuItem>
            
    </Menu>
</UserControl>
